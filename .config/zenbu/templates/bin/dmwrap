#!/usr/bin/bash
#dmwrap - dmenu wrapper

font="{{ font1 }}"
nb="{{ bgc }}"
nf="{{ fgc }}"
sb="{{ hint }}"
sf="{{ bgc }}"
w="930"
h="18"
x="40"
y="8"
opt="-fn "$font" -nb "$nb" -nf "$nf" -sb "$sb" -sf "$sf" -w "$w" -h "$h" -x "$x" -y "$y""

dmenu $opt 
