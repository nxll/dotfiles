#!/usr/bin/bash
#dmwrap - dmenu wrapper

font="{{ font1 }}"
nb="{{ bgc }}"
nf="{{ fgc }}"
sb="{{ hint }}"
sf="{{ bgc }}"
w="830"
h="18"
x="140"
y="5"
opt="-fn "$font" -nb "$nb" -nf "$nf" -sb "$sb" -sf "$sf" -w "$w" -h "$h" -x "$x" -y "$y""

dmenu $opt 
